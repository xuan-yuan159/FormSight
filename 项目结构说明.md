# Auto Questionnaire 项目结构说明

## 1. 项目概览

Auto Questionnaire 是一个无后端的 Edge 浏览器扩展 MVP，使用用户自行配置的 OpenAI 兼容接口完成问卷识别和答案生成。

```text
网页页面
  ↓
Content Script 读取 DOM 并显示浮窗
  ↓
Background Service Worker 调用 AI API
  ↓
Content Script 展示结果、询问用户并回填页面
  ↓
用户人工审核和提交
```

## 2. 完整目录

```text
Auto_Questionnaire/
├─ 项目规划.md                         # 产品定位、流程和开发计划
├─ 项目结构说明.md                     # 当前文档
├─ package.json                        # 依赖和开发命令
├─ pnpm-lock.yaml                      # pnpm 依赖锁定文件
├─ pnpm-workspace.yaml                 # pnpm 安全策略配置
├─ tsconfig.json                       # TypeScript 配置
├─ wxt.config.ts                       # WXT 和 Manifest V3 配置
├─ .gitignore                          # 依赖、构建产物和本地文件忽略规则
├─ entrypoints/
│  ├─ background.ts                    # Service Worker 后台入口
│  ├─ content.ts                       # 网页内容脚本入口
│  ├─ popup/
│  │  ├─ index.html                    # 工具栏弹窗 HTML
│  │  ├─ main.ts                       # 工具栏弹窗 Vue 入口
│  │  └─ App.vue                       # 工具栏弹窗界面
│  └─ options/
│     ├─ index.html                    # 完整设置页 HTML
│     ├─ main.ts                       # 设置页 Vue 入口
│     └─ App.vue                       # AI、浮窗和询问模式配置页
├─ src/
│  ├─ shared/
│  │  ├─ types.ts                      # 共享类型、问卷字段和消息类型
│  │  ├─ constants.ts                  # 默认配置、存储键和模式文案
│  │  ├─ storage.ts                    # 浏览器本地配置和临时会话读写
│  │  ├─ messages.ts                   # 扩展消息类型常量
│  │  ├─ prompts.ts                    # 系统提示词和 AI 请求内容组装
│  │  └─ ai-schema.ts                  # AI JSON 响应校验和解析
│  ├─ questionnaire/
│  │  └─ scanner.ts                    # DOM 表单扫描、字段序列化和页面回填
│  └─ ui/
│     └─ FloatingPanel.vue             # 网页右下角浮窗和问答交互
└─ public/                             # 预留静态资源目录
```

## 3. 入口文件说明

### 3.1 `entrypoints/content.ts`

Content Script 会在普通 HTTP/HTTPS 页面中运行，主要负责：

- 读取本地浮窗开关。
- 使用 Shadow DOM 创建样式隔离的浮窗。
- 挂载 `FloatingPanel.vue`。
- 让浮窗运行在当前网页上下文中，从而能够读取和修改 DOM。

当前设置为页面加载后注入。浮窗本身不会自动提交表单，只有用户点击“应用答案”后才会执行填写。

### 3.2 `entrypoints/background.ts`

Background Service Worker 负责：

- 接收 Content Script 发来的 AI 请求。
- 从本地存储读取 AI 配置。
- 调用用户配置的 OpenAI 兼容接口。
- 解析普通响应或 SSE 流式响应。
- 将 AI 状态、增量文本和完成结果推送回当前标签页。
- 处理配置页的连接测试请求。

AI 请求放在后台入口中，是为了避免网页自身的 CORS 限制，也避免让页面脚本直接接触扩展内部通信细节。

### 3.3 `entrypoints/popup/`

工具栏弹窗用于快速操作：

- 查看当前活动页面。
- 开关网页浮窗。
- 提示用户回到网页使用浮窗。
- 打开完整设置页。

### 3.4 `entrypoints/options/`

完整设置页用于配置：

- AI Base URL。
- AI Model。
- API Key。
- Temperature。
- 最大输出 Token。
- 默认询问模式。
- 页面浮窗开关。
- 是否扫描所有普通网页。

设置页还提供 AI 连接测试和清除 API Key 功能。

## 4. 共享模块说明

### 4.1 `src/shared/types.ts`

定义扩展各模块共同使用的数据结构：

- `AIConfig`：AI 服务配置。
- `UserSettings`：用户设置。
- `QuestionnaireField`：页面表单字段。
- `QuestionnaireSnapshot`：一次页面扫描结果。
- `DecisionQuestion`：需要用户回答的决策问题。
- `ResolvedAnswer`：AI 生成或用户确认的答案。
- `QuestionnaireSession`：临时问卷会话。
- `AIStreamRequest` 和 `AIStreamMessage`：后台流式通信消息。

### 4.2 `src/shared/constants.ts`

集中管理默认值：

- 本地存储键名。
- 默认 AI 地址和模型。
- 默认询问模式。
- 三种询问模式的名称和说明。
- 支持的页面字段类型。

### 4.3 `src/shared/storage.ts`

统一封装 `chrome.storage.local`：

- 读取和保存用户设置。
- 保存当前问卷临时会话。
- 清除当前问卷会话。

当前为了兼容性使用本地存储模拟临时工作区。后续可以改成 `chrome.storage.session` 或 IndexedDB。

### 4.4 `src/shared/prompts.ts`

负责组装 AI 请求：

- 问卷识别提示词。
- 根据用户回答补全答案的提示词。
- AI 连接测试提示词。
- 询问模式说明。
- 请求 ID 生成。

提示词明确要求 AI 返回 JSON，不允许执行网页指令或返回 JavaScript；AI 会结合页面可见文本归纳字段，用户未提供的信息由 AI 根据上下文合理推断或生成示例答案。

### 4.5 `src/shared/ai-schema.ts`

使用 Zod 校验 AI 返回结果：

- 删除可能存在的 Markdown 代码围栏。
- 提取 JSON 文本。
- 校验摘要、答案、追问和警告字段。
- 格式不正确时阻止后续填写。

## 5. 页面扫描和回填

### 5.1 `src/questionnaire/scanner.ts`

当前支持：

- 文本输入框。
- 邮箱输入框。
- 数字输入框。
- 多行文本框。
- 原生单选框。
- 原生复选框。
- 原生下拉框。
- 简单 `contenteditable` 区域。

扫描逻辑会读取：

- 题目或标签文本。
- 字段类型。
- 是否必填。
- 当前值。
- 下拉选项。
- `id`、`name`、`aria-label` 等定位信息。

扫描结果会给每个控件添加 `data-aq-field-id`，AI 只返回字段 ID 和答案，实际的 DOM 定位由扩展内部完成。

扫描结果还会保留页面可见文本上下文，交给 AI 识别复杂表单中的字段关系和题目含义。

### 5.2 表单回填

回填逻辑会：

- 对文本框使用原生 value setter。
- 触发 `input` 和 `change` 事件。
- 对下拉框匹配选项文本或 value。
- 对单选和复选控件设置 checked 状态。
- 对 `contenteditable` 区域写入文本并触发输入事件。
- 记录已填写数量和无法填写的字段。

AI 漏答时会按当前值、字段选项和字段类型生成合理兜底值；控件无法识别或操作时才返回人工补充列表。

## 6. 浮窗交互流程

`src/ui/FloatingPanel.vue` 负责完整的页面内交互：

```text
折叠图标
  ↓
扫描页面
  ↓
显示字段数量和字段摘要
  ↓
开始 AI 识别
  ↓
实时显示流式输出
  ↓
AI 归纳字段（默认折叠）
  ↓
突出显示问卷摘要和关键对话问题
  ↓
用户回答
  ↓
AI 补全答案
  ↓
显示待填写答案（默认 3 项，其余可展开）
  ↓
应用答案
  ↓
提示人工审核
```

浮窗状态包括：

- `idle`：等待操作。
- `scanned`：已扫描页面字段。
- `analyzing`：AI 识别中。
- `waiting`：等待用户回答。
- `completing`：根据用户回答补全中。
- `ready`：答案准备完成。
- `filled`：页面回填完成。
- `error`：发生异常。

## 7. AI 请求和实时输出

请求链路如下：

```text
FloatingPanel.vue
  → browser.runtime.sendMessage
  → background.ts
  → fetch(..., stream: true)
  → 解析 SSE data 内容
  → browser.tabs.sendMessage
  → FloatingPanel.vue 实时展示
```

支持两种响应：

- 普通 JSON 响应。
- OpenAI 兼容的 SSE 流式响应。

流式输出可以显示在“实时输出”区域，但最终答案需要等待完整 JSON 返回并经过 Zod 校验后才会应用到页面。

## 8. 本地数据

### 8.1 用户设置

存储键：`aq-user-settings`

包含：

- AI Base URL。
- Model。
- API Key。
- Temperature。
- 最大输出 Token。
- 询问模式。
- 浮窗开关。
- 页面扫描开关。

### 8.2 当前问卷会话

存储键：`aq-current-session`

包含：

- 当前页面标题和 URL。
- 页面字段快照。
- 表单得分。
- AI 摘要。
- 已确认答案。
- 待询问问题。
- 最终答案。
- 创建时间和更新时间。

浮窗中的“清空”按钮会删除该会话。

## 9. 权限说明

`wxt.config.ts` 当前申请：

- `storage`：保存用户配置和临时问卷会话。
- `tabs`：向当前标签页推送 AI 流式结果。
- `http://*/*` 和 `https://*/*`：在普通网页显示浮窗、读取表单和调用配置的 AI 域名。

当前为了实现“所有页面显示浮窗”，网页权限范围较宽。后续可以改成：

- 默认只使用 `activeTab`。
- 用户点击扩展按钮后才扫描当前页面。
- 用户保存 AI Base URL 时再请求对应 API 域名权限。

## 10. 本地运行

### 安装依赖

```powershell
cd F:\LearningNotes\AI-Project\Auto_Questionnaire
pnpm install
```

### 开发模式

```powershell
pnpm dev
```

开发产物通常位于：

```text
Auto_Questionnaire/.output/chrome-mv3/
```

在 Edge 地址栏打开 `edge://extensions/`，开启“开发人员模式”，选择“加载解压缩的扩展”，选中上面的目录。

### 生产构建

```powershell
pnpm build
```

### 打包扩展

```powershell
pnpm zip
```

## 11. 使用前配置

1. 加载扩展后点击 Edge 工具栏中的扩展图标。
2. 打开“完整设置”。
3. 填写 Base URL、Model 和 API Key。
4. 点击“测试连接”。
5. 保存配置。
6. 打开包含标准表单的网页。
7. 点击右下角浮窗。
8. 选择询问模式并开始识别。
9. 在对话框中一次性补充情况和偏好，未提到的内容由 AI 自动补全。
10. 点击“应用答案”，检查页面后手动提交。

## 12. 当前已知边界

- 没有后端，API Key 和问卷会话保存在浏览器本地。
- AI 服务商需要支持 OpenAI 兼容接口和浏览器扩展请求。
- 自定义下拉框、复杂日期控件、跨域 iframe 和 Canvas 问卷暂未专门适配。
- AI 返回异常时会停止填写；AI 漏答字段会由前端生成合理示例值，避免出现空白答案。
- 目前没有自动提交按钮。
- 页面刷新后，当前页面控件引用会失效，需要重新扫描页面。

## 13. 后续建议

优先顺序建议：

1. 增加常见招聘网站和问卷平台的字段适配器。
2. 将临时会话迁移到 `chrome.storage.session` 或 IndexedDB。
3. 增加字段回填后的页面值校验。
4. 增加网站允许列表和禁用列表。
5. 将页面权限从全站访问收敛到用户授权页面。
6. 增加更复杂控件的人工辅助填写，而不是直接追求全自动。
