# 一问成卷（FormSight）图标生成方案

## 先说结论

根据当前代码，项目暂时只需要一类图片：**浏览器扩展图标**。

- 浮窗、弹窗和设置页没有图片容器，现有界面图标全部使用 Lucide，不需要生成插画。
- README 和界面中的“一问成卷 / FormSight”使用文字即可，不需要单独生成字标图片。
- 不需要生成网页背景、Hero 图、功能插画、人物图、宣传海报或单独 Favicon。
- 只生成 1 张透明背景的 1024×1024 主图标，再在本地缩放出扩展所需尺寸。

## 资源对应关系

| 使用位置 | 实际资源 | 是否需要 AI 单独生成 |
| --- | --- | --- |
| 浏览器工具栏 | 主图标缩放版 | 不需要，使用主图标缩放 |
| 扩展管理页 | 主图标缩放版 | 不需要，使用主图标缩放 |
| 浮窗品牌标识 | Lucide 图标 + 中文文字 | 不需要 |
| Popup 和设置页 | Lucide 图标 + CSS | 不需要 |
| README 项目说明 | 文字和代码说明 | 不需要 |
| 社交分享封面 | 当前没有发布需求 | 暂不生成 |

建议最终由同一张主图标生成：`icon-16.png`、`icon-32.png`、`icon-48.png`、`icon-128.png`、`icon-256.png`。不要让 AI 分别生成这些尺寸，否则图形会出现比例和细节不一致。

## 未来接入位置

图片生成并确认后，只需要加入以下目录：

```text
public/
└─ icons/
   ├─ icon-16.png
   ├─ icon-32.png
   ├─ icon-48.png
   ├─ icon-128.png
   └─ icon-256.png
```

然后在 `wxt.config.ts` 的 `manifest` 配置中声明这些尺寸。主图标母版可以放在项目外部的设计素材目录，不必作为扩展运行时资源重复打包。

## 唯一一次 AI 生成提示词

请为浏览器扩展“一问成卷”设计一个**纯图形应用图标**，英文产品名为 FormSight。这个图标是唯一需要生成的母版，将用于浏览器工具栏、扩展管理页和应用标识。

产品功能：AI 理解网页问卷和复杂表单，通过简短对话确认关键信息，根据上下文推断答案，再把答案辅助填写到正确字段。图形需要表达“对话理解 + 表单字段 + 答案落位”，可以用一个简洁的对话气泡轮廓包住 2 到 3 条表单字段线，并通过负空间隐约形成字母 F；不要把多个图标机械拼在一起。

设计要求：现代生产力工具风格，简洁、可靠、友好、轻量科技感，几何化构图，圆角但不幼稚，主体居中，轮廓粗细统一，缩小后仍然清晰。使用深墨蓝作为主色，青绿色作为连接或理解的强调色，最多加入一个很小的暖黄色确认点。透明背景，正方形构图，四周留出安全边距，不包含任何文字。

请只输出一个正面、居中的 1024×1024 图标母版，透明背景，单一图形主体，边缘清晰，适合后续缩放为 16×16、32×32、48×48、128×128 和 256×256 像素。不要输出 Logo 展示墙、多个候选方案、手机样机、浏览器界面、海报、宣传图或不同尺寸拼图。

## 负面提示词

不要中文或英文文字、不要水印、不要机器人头像、不要人脸、不要普通问号、不要单纯勾选清单、不要文件夹、不要复杂表格截图、不要键盘鼠标、不要地球火箭盾牌、不要紫色霓虹、不要赛博朋克、不要 3D 金属、不要玻璃拟态、不要发光、不要复杂渐变、不要细碎线条、不要多主体、不要边框、不要投影、不要低分辨率、不要模糊边缘、不要拼写错误。

## 生成后的本地处理

AI 只生成主图后，用本地图片工具等比例缩放并导出以下尺寸：

- `16×16`：浏览器工具栏
- `32×32`：高 DPI 工具栏和快捷入口
- `48×48`：扩展管理页
- `128×128`：安装页和扩展详情
- `256×256`：保留高清母版或商店素材

缩放时保持透明背景和原始比例，不要再次交给 AI 重绘。Logo 文字“一问成卷”和“FormSight”直接由网页字体或 SVG 文字呈现，避免文字生成错误，也节省一次图片生成费用。
